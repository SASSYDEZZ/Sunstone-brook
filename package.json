{
  "name": "livingcityengine",
  "private": true,
  "version": "0.1.0",
  "description": "LivingCityEngine — a modular, mobile-first city simulation engine built with TypeScript and Babylon.js.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
